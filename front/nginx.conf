server {
    listen 80;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri /index.html =404;
    }

    location /client.apk {
        alias /shared/client.apk;
    }

    location /shared/ {
        alias /shared/;
        autoindex on;
    }
}
